import type { CompanyListItem } from "../company.types";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { CompanyActiveForm } from "./company-active-form";
import { CompanyFormDialog } from "./company-form-dialog";
export function CompanyList({ companies }: { companies: CompanyListItem[] }) { return <Card><CardHeader className="flex-row items-center justify-between border-b"><div><CardTitle className="text-base">회사</CardTitle><p className="mt-1 text-xs text-muted-foreground">숙소를 소유하는 운영 주체입니다.</p></div><CompanyFormDialog /></CardHeader><CardContent className="p-0">{companies.length === 0 ? <div className="p-5 text-sm text-muted-foreground">등록된 회사가 없습니다. 숙소 등록 전에 회사를 추가하세요.</div> : <div className="divide-y">{companies.map((company) => <div key={company.id} className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2"><p className="font-medium">{company.name}</p><Badge variant={company.isActive ? "secondary" : "outline"}>{company.isActive ? "활성" : "비활성"}</Badge><span className="text-xs text-muted-foreground">숙소 {company.propertyCount}개</span></div><div className="flex gap-2"><CompanyFormDialog company={company} /><CompanyActiveForm id={company.id} isActive={company.isActive} /></div></div>)}</div>}</CardContent></Card>; }
