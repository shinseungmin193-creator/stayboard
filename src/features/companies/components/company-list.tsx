import { useTranslations } from "next-intl";import type { CompanyListItem } from "../company.types";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { CompanyActiveForm } from "./company-active-form";
import { CompanyFormDialog } from "./company-form-dialog";
export function CompanyList({ companies }: {companies: CompanyListItem[];}) {const i18n = useTranslations();return <Card><CardHeader className="flex-row items-center justify-between border-b"><div><CardTitle className="text-base">{i18n("auto.m0107")}</CardTitle><p className="mt-1 text-xs text-muted-foreground">{i18n("auto.m0307")}</p></div><CompanyFormDialog /></CardHeader><CardContent className="p-0">{companies.length === 0 ? <div className="p-5 text-sm text-muted-foreground">{i18n("auto.m0308")}</div> : <div className="divide-y">{companies.map((company) => <div key={company.id} className="flex flex-col gap-3 p-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-2"><p className="font-medium">{company.name}</p><Badge variant={company.isActive ? "secondary" : "outline"}>{company.isActive ? i18n("auto.m0210") : i18n("auto.m0115")}</Badge><span className="text-xs text-muted-foreground">{i18n("common.property")}{company.propertyCount}{i18n("auto.m0271")}</span></div><div className="flex gap-2"><CompanyFormDialog company={company} /><CompanyActiveForm id={company.id} isActive={company.isActive} /></div></div>)}</div>}</CardContent></Card>;}
