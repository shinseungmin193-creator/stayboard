"use client";
import { useActionState, useState } from "react";
import { ActionMessage } from "@/components/shared/action-message";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import type { ActionResult } from "@/lib/action-result";
import type { CompanyMemberRole } from "@/lib/generated/prisma/enums";
import { setCompanyMemberActiveAction, updateCompanyMemberAction } from "../member-management.actions";
import type { AwaitedReturn, ManagedMembership } from "../member-management.types";
const initial: ActionResult = { success: false, message: "" }; const date = new Intl.DateTimeFormat("ko-KR", { dateStyle: "short", timeStyle: "short", timeZone: "Asia/Tokyo" });
function MemberCard({ membership, companyId, properties, actorUserId }: { membership: ManagedMembership; companyId: string; properties: Array<{ id: string; name: string }>; actorUserId: string }) { const [updateState, updateAction, updatePending] = useActionState(updateCompanyMemberAction, initial); const [activeState, activeAction, activePending] = useActionState(setCompanyMemberActiveAction, initial); const [role, setRole] = useState<CompanyMemberRole>(membership.role); const isSelf = membership.user.id === actorUserId; return <article className="min-w-0 rounded-xl border p-4"><div className="flex flex-wrap justify-between gap-2"><div className="min-w-0"><p className="truncate font-semibold">{membership.user.name}</p><p className="truncate text-sm text-muted-foreground">{membership.user.email}</p><p className="text-xs text-muted-foreground">최근 로그인 {membership.user.lastLoginAt ? date.format(membership.user.lastLoginAt) : "없음"} · 가입 {date.format(membership.createdAt)}</p><p className="mt-1 text-xs text-muted-foreground">접근 숙소 {membership.role === "ADMIN" ? "전체" : `${membership.propertyAccesses.length}개`}</p></div><div className="flex gap-1"><Badge variant="outline">{membership.role === "ADMIN" ? "관리자" : "직원"}</Badge><Badge variant={membership.status === "ACTIVE" ? "secondary" : "outline"}>{membership.status === "ACTIVE" ? "활성" : "비활성"}</Badge></div></div><details className="mt-3"><summary className="min-h-11 cursor-pointer content-center text-sm font-medium">권한 및 상태 관리</summary><div className="mt-2 space-y-3"><form action={updateAction} className="space-y-3 rounded-lg border p-3"><input type="hidden" name="companyId" value={companyId} /><input type="hidden" name="membershipId" value={membership.id} /><Input name="name" defaultValue={membership.user.name} aria-label="구성원 이름" required maxLength={50} /><select name="role" value={role} onChange={(event) => setRole(event.target.value as CompanyMemberRole)} disabled={isSelf} className="h-11 w-full rounded-lg border bg-background px-3 text-sm md:h-8"><option value="ADMIN">관리자</option><option value="STAFF">직원</option></select>{role === "STAFF" && <fieldset><legend className="mb-1 text-xs font-medium">접근 가능한 숙소</legend><div className="grid max-h-52 gap-1 overflow-y-auto rounded-lg border p-2 sm:grid-cols-2">{properties.map((property) => <label key={property.id} className="flex min-h-11 min-w-0 items-center gap-2 rounded px-2 text-sm hover:bg-muted"><input className="size-5 shrink-0" type="checkbox" name="propertyIds" value={property.id} defaultChecked={membership.propertyAccesses.some((item) => item.propertyId === property.id)} /><span className="truncate">{property.name}</span></label>)}</div></fieldset>}<Button type="submit" size="sm" disabled={updatePending || isSelf}>권한 저장</Button><ActionMessage state={updateState} /></form><form action={activeAction} onSubmit={(event) => { if (membership.status === "ACTIVE" && !window.confirm("이 구성원을 비활성화하시겠습니까?")) event.preventDefault(); }}><input type="hidden" name="companyId" value={companyId} /><input type="hidden" name="membershipId" value={membership.id} /><input type="hidden" name="active" value={String(membership.status !== "ACTIVE")} /><Button type="submit" size="sm" variant="outline" disabled={activePending || isSelf}>{membership.status === "ACTIVE" ? "구성원 비활성화" : "구성원 재활성화"}</Button><ActionMessage state={activeState} /></form></div></details></article>; }
export function MemberManagement({ result, companyId, properties, actorUserId }: { result: AwaitedReturn; companyId: string; properties: Array<{ id: string; name: string }>; actorUserId: string }) { return <Card><CardHeader><CardTitle>구성원 목록 · {result.totalCount}명</CardTitle></CardHeader><CardContent><div className="grid gap-3 lg:grid-cols-2">{result.rows.map((row) => <MemberCard key={row.membership.id} membership={row.membership} companyId={companyId} properties={properties} actorUserId={actorUserId} />)}</div>{!result.rows.length && <p className="py-12 text-center text-sm text-muted-foreground">조건에 맞는 구성원이 없습니다.</p>}</CardContent></Card>; }
